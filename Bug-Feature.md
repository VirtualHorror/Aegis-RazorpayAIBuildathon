# Bug / Feature tracker — scope → build → verification

> Every feature and bug has a row. Status: `scoped` → `in_progress` → `implemented` → `verified` (only the Verification Agent sets `verified`, citing evidence).
> Evidence = the command run + observed output, or a screenshot path. "It works" is not evidence.
> The file name is `Bug-Feature.md` because `/` cannot appear in a file name (D-021).

## Features

| ID | Feature | Task | Owner | Status | Verification evidence |
|---|---|---|---|---|---|
| F-001 | Bare-metal environment (Node 24, pnpm 11, PostgreSQL 16 bootstrap script) | T1 | Claude | verified (Postgres part pending) | `node -v` → v24.20.0, `pnpm -v` → 11.25.0; `scripts/bootstrap-system.sh` passes `bash -n`; PostgreSQL install awaits the human `sudo` step (DV-001) |
| F-002 | Monorepo scaffold (api, web, shared), typecheck + tests green | T1 | Claude | verified | `pnpm typecheck` / `test` (23 tests) / `lint` / `next build` all green; `/health` degraded-but-up without Postgres — see `TestChecklist.md` §T1 |
| F-003 | Footer + theme toggle in the web shell | T1 | Claude | verified | `curl localhost:3000` contains the footer text, `suppressHydrationWarning`, the theme radiogroup and `₹4,20,000.00` from `@aegis/shared` |
| F-004 | Migration runner + core schema + seed | T2 | Codex | verified | Claude re-ran `TestChecklist.md` §T2 on 2026-09-05: `db:migrate:down` ×2 → only `schema_migrations` left → `db:migrate` → `applied 2 migration(s)`, `\dt` = 21 relations; `pnpm db:migrate -- --status` → `pending: (none)`; `db:seed` ×2 → identical counts 12/16/12/3/4/6; `0001_init.sql` diffed byte-for-byte against Checklist 2.1 (identical, 21 `-- Intent:` comments); readonly role: 18 tables SELECT-only, exact non-PII column lists on `customers`/`payments`, `SELECT email\|name\|contact\|notes\|*` denied, writes rejected (read-only transaction), `pg_sleep(6)` cancelled at 5 s; after `db:migrate:down` the role holds 0 table/column privileges; missing-role branch of 0002 up/down emits `WARNING … skipping` and exits 0; runner forwards `RAISE WARNING` to `log.warn`; API exits 1 on a pending migration and `/health` → `{"status":"ok","db":"ok"}` after migrating; `pnpm typecheck && pnpm test && pnpm lint` green (28 tests) |
| F-005 | Idempotent webhook ingress (HMAC, dedupe, outbox) | T3 | Codex | verified | Claude re-ran `TestChecklist.md` §T3 on 2026-09-05 after fixing B-004: `pnpm typecheck && pnpm test && pnpm lint` green (17 shared + 29 api tests); ingress integration 10/10 including 20 concurrent identical POSTs → 1 row, `duplicate_count=19`, 1 job, and the new B-004 regression test (red-green proved by reverting `ingressKey`); live HTTP probe on port 4021 → forged 401 quarantined as `unverified:4fbe…`, genuine `evt_live_victim` accepted with exactly 1 `process_event` job, retry → `duplicate_count=1`; HMAC verified over raw bytes whose re-serialisation differs (C-D2), `timingSafeEqual` on equal-length buffers (C-D1), zero LLM calls in the routing layer (C-A1/C-A2) |
| F-006 | Job worker (SKIP LOCKED, backoff, DLQ, sweeper) + entity projections with precedence | T4 | Codex | verified | `pg_isready` accepting; migration status has `0001_init`, `0002_readonly_grants`, `0003_projection_guards` with no pending files; worker integration 1 file / 12 tests and projection integration 1 file / 7 tests passed; full `pnpm test` 9 API files / 51 tests plus shared 25 tests, `pnpm typecheck`, and `pnpm lint` all passed; live API proof on port 4024 accepted `evt_task4_live`, then observed `succeeded|processed|captured|2|1|evt_task4_live` and one customer/order/payment parent each (see `TestChecklist.md` §T4 and `HANDOFF5.md`). Claude re-ran §T4 on 2026-09-05 after fixing B-006: `pnpm db:migrate -- --status` → three applied, `pending: (none)`; `pnpm typecheck && pnpm test && pnpm lint` green (25 shared + **52** api tests, up one for the B-006 regression); claim SQL matches Checklist 4.1 byte-for-byte; `process_event` re-reads `signature_valid` under `FOR UPDATE` before parsing (D-034) and an invalid row leaves zero projection rows with the job `succeeded` and the event `ignored`; `assertSafePaise` in `packages/shared/src/money.ts` is untouched by the T4 diff — the int8 parser lives in the side-effect module `apps/api/src/db/pg-types.ts`, imported by `db/pool.ts` and loaded by Vitest `setupFiles`; zero LLM calls in the routing/state layer (C-A1/C-A2); live probe on port 4031 delivered `order.paid` + `payment.captured` for one order/customer concurrently → both jobs `succeeded` at `attempts=1`, `order paid v1`, `payment captured v1`, customer `IN`/`en-IN`, zero `40P01` in the log; stopped with `kill -TERM 88476` |
| F-007 | Webhook simulator CLI (scenarios, duplicates, bursts, chaos) | T5 | Codex | verified | Claude re-ran `TestChecklist.md` §T5 on 2026-09-05 on port 4061: `tsc --noEmit --listFiles` lists all 9 `scripts/` files and `eslint --format json` lints the same 9 with 0 errors (C-E2/B-003); shared scenario suite 22 tests, API signer + route-guard suite 5 tests; `all --seed aegis-review-t5` -> accepted=12 duplicate=0 rejected=1 ignored=1 with `bad_signature namespace=unverified:aa84452c...` (C-C1/D-031, D-038); `payment_failed_3ds_intl --dupes 2` -> accepted=1 duplicate=2; `burst --burst 50` -> 50/50 succeeded, `max_attempts=1`; `--burst 400` -> accepted=236 rate_limited=164 rejected=0, 429s totalled separately (D-040); unreachable `--api` -> exit 1; unknown_event with an unusable `DATABASE_URL` fails closed (exit 1) rather than guessing `ignored`; `POST /api/v1/sim/run` returns the same classification in development and 404 in production (C-D5). Claude added `--contend`, `verifyOriginalDeliveries`, the dev-route delivery cap, and their tests during verification; the contended burst immediately found B-007. `pnpm typecheck && pnpm test && pnpm lint` green (shared 5 files/47 tests, api 10 files/55 tests). API stopped with `kill -TERM`; port 4000's unrelated process left alone |
| F-008 | LLM client abstraction (anthropic/openai/stub + resilient wrapper + prompt registry) | T6 | Codex | verified | Claude re-ran `TestChecklist.md` §T6 on 2026-09-05. `pnpm typecheck && pnpm test && pnpm lint` green **three consecutive times** after fixing B-008 (shared 5 files/47 tests; api 18 files/**89** tests, up four for the new chaos suite). `@anthropic-ai/sdk@0.123.0` really exports `helpers/zod` `zodOutputFormat(zodObject: z.ZodType)` — the adapter uses a real API, not an invented one. `AEGIS_LLM_PROVIDER=stub pnpm --filter @aegis/api llm:smoke` → `provider=stub model=fixture-v1` + a schema-validated `THREE_DS_AUTH_FAILED`/`RETRY_LINK_LOCALIZED` sample. Live API on port 4088 logged `llm provider selected {provider: stub}` exactly once at boot and served `GET /api/v1/system` → `{"provider":"stub","model":"fixture-v1","modelFast":"fixture-v1"}` (no credentials in the body); stopped with `kill -TERM 183183`, port confirmed closed. Fail-closed simulator re-proved with an unreachable `DATABASE_URL` → `could not verify unknown_event evt_0002_27f22d3d …; DATABASE_URL is required for ignored classification`, exit 1. C-D3 grep over the diff found no `sk-`/`whsec_`/credentialed `postgres://`. Claude fixed the duplicated diagnosis contract (D-045) and added `apps/api/test/chaos.integration.test.ts` during verification. Anthropic and OpenAI remain SDK-mock-tested only; **no live Anthropic or OpenAI output is claimed anywhere in this task.** |
| F-009 | Diagnostician (hints → LLM → cross-check → fallback) | T7 | Codex | verified | Claude re-ran `TestChecklist.md` §T7 on 2026-09-05. Focused suite 6 files / 44 tests; `pnpm typecheck && pnpm test && pnpm lint` green **three consecutive times** (shared 5 files / 47 tests; api 23 files / **130** tests, up two for the fixes below). **C-A7**: masking the real snapshot shape (`RETURNING *` payments row + `CustomerRow`) leaves no raw email, phone, or full name in the prompt, and nested `card.number`/`cvv`/`expiry_*` are digit-masked at any depth. **PostgreSQL numeric trap**: proved non-vacuous — a live driver read of `diagnoses.confidence` returns the **string** `"0.900"` (no `setTypeParser(1700)` anywhere; `pg-types.ts:8` only registers OID 20), so `parseDiagnosisConfidence` (`apps/api/src/db/repos/diagnoses.ts:76`) is load-bearing and every read path goes through `mapDiagnosisRow`. **C-A6**: `Diagnostician.db` is typed `Pool`, not `PoolClient`, so a transaction client cannot be injected, and the model call precedes `insertDiagnosis`; Claude added a recording-pool ordering test that pins the trace to `['llm','sql:INSERT']` with no `BEGIN`. **D-045**: `ROOT_CAUSES`/`STRATEGIES`/`DiagnoseOutputSchema` are defined once in `apps/api/src/llm/prompts/index.ts`; `diagnosis/schema.ts` and `stub-fixtures.ts` re-export, nothing restates. All eight cross-check rows have a test (row 5 via `it.each`). Claude fixed B-010 during verification |
| F-010 | EventOrchestrator + ActionModule interface + actions audit + SSE bus | T8 | Codex | verified | Focused integration: `pnpm --filter @aegis/api exec vitest run src/orchestrator src/guardrails src/bus src/modules/checkout-recovery test/orchestrator.integration.test.ts src/orchestrator/projections/projections.test.ts test/chaos.integration.test.ts src/worker/process-event.test.ts --no-file-parallelism --maxWorkers=1` -> 10 files / 41 tests passed; covers deterministic routing, module call order, all four global guardrails, prior-failure wiring, action idempotency/advisory serialization, B-007 order-slot reservation, B-009 worker chaos re-entry, SSE framing/CORS, and audited execution. Three final full repository gates also passed: API 31 files / 160 tests, shared 5 files / 47 tests, typecheck and lint green | Claude re-verified 2026-09-05: reviewed the full `task-07-done..HEAD` diff against C-A2/C-A4/C-C3/C-B1/C-D5. Confirmed **zero LLM calls inside any transaction** — the only `withTransaction`/`BEGIN` sites are `EventOrchestrator` (projection, event status, proposal persistence), `execute.ts` (two short `withClientTransaction` phases around `module.execute`), `reconciler.ts` and `process-event.ts`; `Diagnostician.diagnose` opens none and `insertDiagnosis` runs on the pool after the model call. Re-ran the focused suite myself: 10 files / 41 tests passed; `pnpm typecheck && pnpm test && pnpm lint` exit 0 (shared 5 files / 47 tests, API 31 files / 160 tests, lint 3 projects) |
| F-011 | CheckoutRecovery module (localised WhatsApp retry link) | T9 | Codex | verified | `pnpm --filter @aegis/api exec vitest run src/modules/checkout-recovery/index.test.ts src/modules/checkout-recovery/templates.test.ts src/modules/checkout-recovery/links.test.ts --no-file-parallelism --maxWorkers=1` -> 3 files / 9 tests passed: deterministic daily payment retry links, locale/template selection (including Hindi), WhatsApp schema validation, masked recipient, safe paise validation, and opted-out guard. The same three final full repository gates passed: API 31 files / 160 tests, shared 5 files / 47 tests, typecheck and lint green | Claude re-verified 2026-09-05: module has no transaction and no model call; `execute`/`compensate` touch the pool only. Covered by the same green focused suite (10 files / 41 tests) and the three-project full gate (exit 0) |
| F-012 | SubscriptionSalvager module (dunning state machine) | T10 | Codex | verified | Focused salvager suite: 2 files / 6 tests passed; deterministic FSM, bounded entity updates, synthetic retry path and recovery cancellation | Claude re-verified 2026-09-06: `runDunningRetry` (`modules/subscription-salvager/index.ts:132-164`) holds `subscriptions FOR UPDATE` with zero model calls and commits before `handleSynthetic`; the salvager has no LLM dependency at all |
| F-013 | B2BNegotiator module (bounded negotiation) | T11 | Codex | verified | Pricing/state tests and module suite pass; live LLM path is injected at registry boot and model-down fallback is bounded | Claude re-verified 2026-09-06: the only model call is in `propose()` (`modules/b2b-negotiator/index.ts:106`), invoked by `runActionPath` on the pool, outside every `withTransaction`; persistence happens afterwards in `persistProposal` |
| F-014 | ChargebackEvidence module (packet + human review) | T12 | Codex | verified | Assembly/module integration: 2 files / 4 tests passed; packet persistence is proposal-hook based and execution refuses unapproved packets | Claude re-verified 2026-09-06: narrative model call in `propose()` (`modules/chargeback-evidence/index.ts:49`) runs before `onProposed` persists the packet after the action row commits (D-053); no lock is held across it |
| F-015 | Approvals API + ledger + recovery attribution + metrics | T13 | Codex | verified | `approvals.integration.test.ts`: concurrent approvals return one 200/one 409 and execute once; concurrent capture/order attribution creates one action-referenced credit and updates `recovered_paise`; metrics route reads independent aggregates | Claude re-verified 2026-09-06: `attribution.ts:68` and `approvals.ts:127,162` take `FOR UPDATE` in short transactions with no model call; `decideAction` commits before `executeAction` (D-054). `0004_ledger_unique.sql` is the only new migration and follows `0003` without collision. Full gate re-run by Claude 2026-09-06 (`pnpm typecheck && pnpm test && pnpm lint`, exit 0): shared 5 files / 47 tests, API 48 files / 210 tests, lint green in all three projects |
| F-016 | x402 gateway (challenge, verify, settle, replay guard, caps) | T14 | Codex | verified | `src/x402` tests: 3 files / 9 tests; nonce row locking, exact 402/200 wire shapes, HMAC tamper rejection, expiry, caps, kill switch, and concurrent settlement are covered | Claude re-verified 2026-09-06: `facilitator.ts:27` locks `x402_payments.nonce FOR UPDATE` in a pure-TypeScript transaction; no migration was needed (`nonce` already UNIQUE, DV-009) |
| F-017 | Ask Aegis: Text-to-SQL sandbox + deterministic forecaster | T15 | Codex | verified | `src/nlq` tests: 3 files / 14 tests; AST allowlist/denylist, readonly timeout path, SQL persistence, and OLS+MA7 fixture pass | Claude re-verified 2026-09-06: all three `completeJson` calls (`nlq/service.ts:110,149,175`) run on the pool; `executeReadonly` opens the readonly transaction only after validation and holds no row lock (`SET LOCAL statement_timeout = '5000ms'`) |
| F-018 | Compliance scanner (keywords + LLM rubric + evidence check) | T16 | Codex | verified | `src/compliance` tests: 3 files / 9 tests; rubric validation, evidence-span and keyword/model disagreement review, LLM-down fallback, and concurrent one-flag-per-run behavior pass | Claude re-verified 2026-09-06: `scanner.ts:63` classifies before `upsertFlag` opens `BEGIN` → advisory lock → `FOR UPDATE` (`scanner.ts:113-117`); the model is never called with a lock held |
| F-019 | Web shell: design system, nav, theme, footer, SSE hook, GlowInput | T17 | Claude | implemented | `pnpm --filter @aegis/web build` → 11 routes compiled (`/` dynamic, the rest static); `pnpm --filter @aegis/web lint` clean; `pnpm --filter @aegis/web test` → 2 files / 8 tests (format helpers, SSE backoff/buffer/parse/filter); `pnpm --filter @aegis/api exec vitest run src/routes/system.test.ts src/bus` → 2 files / 3 tests after the `/api/v1/system` env/version fields. Chrome 2026-09-06: footer text present on `/`, `/events`, `/kitchen-sink`; theme toggle → `localStorage.theme=light`, `<html class="light" style="color-scheme: light">` survives reload; top-bar pills read the live API (`LOCAL · SIM`, `openai · gpt-5.6`, `Kill switch off`, `Live`); GlowInput Tab fills the example and Enter submits; Drawer closes on Escape and returns focus to its trigger; 360px iframe: `scrollWidth 342 ≤ innerWidth 357`, sidebar hidden, bottom bar shown, table scrolls inside its card |
| F-020 | Overview + Events pages | T18 | Claude | implemented | `pnpm --filter @aegis/web test` → 5 files / 22 tests (adds `lib/events.test.ts`, `lib/overview.test.ts`, `components/fx/halftone.test.ts`); `pnpm --filter @aegis/api exec vitest run test/approvals.integration.test.ts src/routes` → 2 files / 7 tests (B-012 cursor and B-013 call-count regressions). Chrome 2026-09-06: Run demo from the top bar → toast "Demo sent 14 webhooks: 12 accepted, 0 duplicate, 1 rejected, 1 ignored, p95 18 ms"; `/events` prepended the 14 rows live with the family-colour rail flash and the halftone pulsed; `/` counters moved from 1,907 → 1,917 events and 0 → 6 executed / 15 blocked, the recent-actions list filled from `action.proposed`/`action.executed`, and the prism labels lit per module. 360px iframe: `/events` renders 50 cards (`ul.sm:hidden`), `/` stacks the hero copy above the picture, both `scrollWidth 342 ≤ innerWidth 357` |
| F-021 | Actions + Approvals pages | T19 | Claude | implemented | `pnpm --filter @aegis/web test` → 6 files / 27 tests (adds `lib/actions.test.ts`: stream merge with filters, pending queue, guard-value formatting, timeline ordering); `pnpm --filter @aegis/api exec vitest run test/approvals.integration.test.ts src/orchestrator/projections src/modules/b2b-negotiator` → the actions list now returns `diagnosis_provider`/`diagnosis_degraded` and `merchantFloorPaise` is unit-tested. Chrome 2026-09-06: `/actions` listed 24 rows with module rails, status pills, impact/expected in ₹ and a per-row diagnosis column; the drawer showed proposal, 8-rule bounds checklist (limit vs actual), diagnosis card (`openai gpt-5.6-luna`, cross-check "agreed"), the exact WhatsApp payload under a SIMULATED watermark, and the timeline; `/approvals` showed the ₹3,99,000 offer against its 10 passed rules, Approve with the note "Verified the floor and the 5 percent round-1 discount…" returned 200 and toasted "Approved … now executed. Recorded as human:dashboard", and the row left the queue |
| F-022 | Ask Aegis + Compliance pages | T20 | Claude | implemented | `pnpm --filter @aegis/web test` → 8 files / 32 tests (adds `components/ask/ask.test.ts` for SQL tokenising and chart scaling, `components/compliance/compliance.test.ts` for verbatim-span highlighting). Chrome 2026-09-06 against the live proxy model `gpt-5.6-luna`: "How much revenue did we recover this week by module?" → generated SQL shown and `validated`, 0 rows, AI-written summary, 5.3 s; "Forecast recovered revenue for the next 7 days" → deterministic metric SQL, history + dashed forecast + residual band, `ols+ma7`, r² 1.00, numbers table; "Show me everything in information_schema.tables" → `rejected` with "schema is not allowed: information_schema" and nothing executed. Compliance: Run scan → 16 products scanned, 5 flags, 0 degraded, provider `openai gpt-5.6-luna`; each card highlights the model's evidence span inside the product's own copy (`Lottery ticket bundle with a chance to win a cash prize.`), shows keyword-versus-model agreement and the model reasoning; Acknowledge posted the status and the card updated to "Acknowledged · Last set by human:dashboard" |
| F-023 | x402 Lab + Settings pages | T21 | Claude | implemented | `pnpm --filter @aegis/api exec vitest run test/approvals.integration.test.ts` → 10 tests, adding the signing-helper test (challenge → sign → 200 → replay 402, secret never in the response) and the kill-switch publish test; `pnpm --filter @aegis/web test` → 9 files / 36 tests with `lib/guardrails.test.ts` (rupee→paise, bounds, quiet-hour and schedule notation). Chrome 2026-09-06: `/x402` ran the three steps live — 402 with price/payTo/nonce, 200 with the decoded `X-PAYMENT-RESPONSE`, then `{"error":"nonce_already_settled"}`, and the settlement table showed the new row as `Settled` beside an older `Rejected · Amount exceeds policy`; caps panel read ₹1,000 per request and ₹5,000 per payer per day from the guardrails. `/settings`: 120 was refused client-side ("Enter a percentage between 0 and 100"), 12 saved and the field showed "Stored: 12 · last set by human:dashboard"; the kill switch confirmed through a custom dialog, turned the top-bar pill red live over SSE, and the change history showed `true → false` by `human:dashboard`. `/subscriptions` and `/invoices` render their state and the invoice floor |
| F-024 | Flourishes: PrismHero (vgpu + 2D fallback), HalftoneField, polish, a11y | T22 | Claude | implemented | `pnpm --filter @aegis/web test` → 11 files / 45 tests, adding `prismGeometry.test.ts` (rest angle, pointer bounds, Snell bend, 18° fan across seven modules, in-frame at 360/960/1920 px) and `prismUniforms.test.ts` (hex parsing, shader packing, pulse clamping). `npx vgpu check src/components/prism/prism.wgsl` → `diagnostics: []` and the `params` binding reflected; device validation skipped (`VGPU-WGSL-VALIDATE-NO-DEVICE`) because this VM has no adapter, recorded in DV-010. Chrome 2026-09-06: the hero renders, the beam follows the pointer and the fan sweeps with it, the legend lights per module on live events; `navigator.gpu` exists here but `requestAdapter()` returns null, so the Canvas 2D path took over and logged why — the fallback proven live (C-F4). Lighthouse accessibility: `/` desktop **100** (0 failed), `/approvals`, `/events`, `/x402` mobile **100** |
| F-025 | Demo storyboard script, README, video plan | T23 | Claude | implemented | `bash scripts/demo.sh` completes in 1:46 with every read-back from the current run (`discount_offer: blocked · ₹21000 · daily_discount_budget_paise`, `evidence_packet: pending_approval`, `recovery action executed: whatsapp_cart_nudge`, `recovered_paise` rising 79 900 per run); `README.md`, `docs/video-storyboard.md`, `docs/architecture.svg` written |
| F-026 | Hardening: security review, failure drills, final test pass | T24 | Claude | implemented | Constraints §D re-verified live (C-D1…C-D7 below); drills run with real output: LLM down, every DB connection severed with 242 jobs in flight (no job lost, `dead_letter=0`), duplicate storm, kill switch on/off; `pnpm test && typecheck && lint && web build` green; tagged `v1.0.0` |
| F-027 | Sandbox / BYOK mode: Demo ↔ Live modal, per-account webhook secret verified at ingress, caller-supplied model key per request | T25 | Claude | implemented | `pnpm --filter @aegis/api exec vitest run src/sandbox src/llm/byok.test.ts src/llm/factory.test.ts test/ingress.integration.test.ts test/sandbox.integration.test.ts` → 5 files / 42 tests, each new test watched red before green (the two leak tests were proven non-vacuous: before the guardrails filter and migration 0005 they failed with `13 ≠ 12` rows and `readonly sees the secret`); `pnpm --filter @aegis/web test` → 12 files / 48 tests; `pnpm --filter @aegis/web build` → exit 0. Live API on port 4000: `POST /api/v1/sandbox/keys` → `{"sandbox":{"key":"sandbox_secret_acc_LiveCurl1","webhook_secret_fingerprint":"961ed1f72616"}}` (no secret in the body); a delivery for that account signed with the tenant secret → `200 accepted`, the same bytes signed with the `.env` secret → `401 invalid_signature` keyed `unverified:…`; `GET /api/v1/guardrails` lists 0 sandbox rows; `aegis_readonly` sees 12 guardrails and 0 sandbox rows (RLS); `audit_log` holds `sandbox.keys_saved` with the fingerprint only. Chrome 2026-09-06: top-bar pill opens the modal (focus on the first radio, Escape closes, portalled to `body`); empty Live save shows both field errors with `aria-invalid` and focuses the account field; Save → toast `Live mode on for acc_ChromeLive1 … fingerprint fd467aa991ac`, pill `Live · acc_ChromeLive1`, `localStorage.aegis.sandbox` set; `/ask` in Live mode with a fake key → `openai_AuthenticationError: 401 Invalid API key`, answered 200 as `degraded` (the key went to OpenAI, not the deployment proxy; C-A4 fallback intact) and every GET grew an `OPTIONS 204` preflight (the custom header through CORS); back in Demo mode the same question `executed`, 1 row, and the history GET had no preflight (no header sent); Forget keys → storage `null`, pill `Demo mode`. 360px iframe: `scrollWidth 345 ≤ innerWidth 360` with the icon-only pill; light theme modal readable; footer present |
| F-029 | Prism hero renders the reference-grade 3D glass prism on the WebGPU path | hero pass | Claude | implemented | `npx vgpu check src/components/prism/prism.wgsl` → `validation ok`, 0 diagnostics. Headless pixels through vgpu's own software renderer (`npx vgpu install-software-renderer`, then `vgpu/node` + `resolveShader` + `target.read()` driving the repo's own `prismGeometry`/`prismUniforms`): before the rewrite the frame was `mean 88.98 / max 229 / near-black 0.0000` — a washed field with no black and no highlight; after it is `mean 53.55 / max 255 / lit 0.69`, and the same run at 360×200 and with three module pulses lit renders correctly. Chrome (headless, SwiftShader adapter) on the production build: hero canvas 990×289 with a live `webgpu` context, screenshot shows the solid prism, the dispersed beam and the seven-ray fan. Same build in a Chrome with no adapter: `[aegis] no WebGPU device on this machine, using the Canvas 2D renderer: navigator.gpu.requestAdapter() returned null.` and the 2D path draws the same scene. `pnpm --filter @aegis/web test` → 13 files / 52 tests |
| F-030 | Prism hero replaced by a strict replica of the vgpu.sh shader: solid SDF glass prism, one white ray, impact bloom, internal travel, exit caustic, continuous spectrum, lit dust, Lottes tonemap, dithering, edge fade | hero replica | Claude | implemented | `npx vgpu check src/components/prism/prism.wgsl` → `validation ok`, 0 diagnostics, one `params` binding. Headless render through vgpu's software renderer: `mean 26.36 · max 254 · lit 0.25 · near-black 0.655` — a black stage with a full-intensity highlight. Dust proven to exist and move by differencing two frames (t=0 vs t=7.5 s): 841 pixels changed, max delta 18, 0.29 % of the frame, everything else in the scene being static. Chrome (headless, SwiftShader): hero canvas 990×289 backing on a 990×289 box, live `webgpu` context, screenshot shows the prism, the beam and the rainbow. `pnpm test` → 68 files / 356 tests; typecheck, lint, web build all exit 0 |
| F-031 | The hero beam follows the pointer, with the safe steering range derived inside the shader | hero replica | Claude | implemented | `npx vgpu check` → validation ok, 0 diagnostics, `params` now carries `resolution_time` and `pointer`. Headless renders at the four pointer corners and at rest: saturated ('spectrum') pixels 10 071 / 19 699 / 11 092 / 12 926 / 15 981 — never zero, so no reachable pointer position sends the light past the critical angle; the analytic floor the shader computes (16.87°) matches a CPU sweep of the same geometry to the degree. Chrome (headless, SwiftShader) on the production build with dispatched mouse events: hero box 990×289, cursor at 8 %/12 % versus 92 %/90 % changes 107 875 pixels (37.8 % of the hero, max delta 247) — the beam sweeps, the entry point slides and the rainbow follows. `pnpm test` → 68 files / 357 tests; typecheck, lint, build all exit 0 |
| F-032 | Prism hero rebuilt as a multi-pass mesh pipeline: environment map, mesh beam and glass, bloom, composite | hero replica | Claude | implemented | `npx vgpu check` on all seven shaders → validation ok, 0 diagnostics. Headless run of the same five passes through vgpu's software renderer: `max 255 · lit 0.21 · near-black 0.768 · 17 449 saturated pixels` at rest, and 12 359 / 13 670 at the two pointer extremes — the spectrum survives everywhere. Chrome (headless, SwiftShader) on the production build with dispatched mouse events: hero 990×289, moving the cursor corner to corner changes 73 949 pixels (25.9 % of the hero, max delta 255). `pnpm test` → 68 files / 358 tests; typecheck, lint, build all exit 0 |
| F-028 | Control-plane authentication: auth on every `/api/v1/*` write, `actor` from the principal, tenant-ownership check on `sandbox_secret_<account_id>` | T26 | — | declined (MVP, D-080) | Declined 2026-09-06 by the project owner: the boundary is documented (`Architecture.md §11`, README "Sandbox / BYOK mode") instead of solved, and the hosted instance is labelled an evaluator sandbox; first post-MVP task. Original review note — raised by the automated security review of T25 (`routes/sandbox.ts`): the route is unauthenticated like every other privileged route (`PUT /api/v1/guardrails/:key`, `POST /api/v1/actions/:id/decision`), and T25 makes that gap sharper because rotating a tenant secret lets a network-local caller forge that account's webhooks. Deliberately not patched onto one route: an auth scheme is cross-cutting and belongs to its own task. Architecture.md §11 states the boundary; README qualifies "deployable" |

## Bugs

| ID | Title | Found in | Root cause | Fix | Status | Evidence |
|---|---|---|---|---|---|---|
| B-001 | API printed `usage: migrate-cli <up\|down\|status>` and exited with code 2 at boot | T1 | `server.ts` imported `MIGRATIONS_DIR` from `db/migrate-cli.ts`, whose top-level `main()` ran on import | moved paths to `apps/api/src/db/paths.ts`; the CLI now runs only when `process.argv[1]` is the CLI file (`migrate-cli.ts:57`) | verified | `tsx src/server.ts` → `aegis api listening on http://0.0.0.0:4000`; `tsx src/db/migrate-cli.ts` → usage, exit 2 |
| B-002 | Documented `pnpm db:migrate -- --status` was treated as `up` | T2 | the API package script supplies `up` before forwarded pnpm arguments, so the original parser never saw `--status` as the command | normalize trailing `--status` arguments in `apps/api/src/db/migrate-cli.ts` and route them to `migrationStatus` | verified | `pnpm db:migrate -- --status` → `applied: 0001_init, 0002_readonly_grants` and `pending: (none)`; re-run by Claude 2026-09-05 from the repo root → `applied: 0001_init, 0002_readonly_grants` / `pending: (none)` |
| B-003 | `db/seed/*.ts` was outside every typecheck and lint program; `tsc` could not resolve `pg`/`dotenv` from `db/` (`TS2307`) | T2 verification (Claude) | `db/` is not a workspace package and `apps/api/tsconfig.json` only included `src`/`test`; the seed ran only because `tsx` resolves bare specifiers from its own install location as a fallback | root devDependencies `pg`, `dotenv`, `@types/pg` (D-030); `apps/api/tsconfig.json` includes `../../db/seed`; the `@aegis/api` lint script covers `../../db/seed` | verified | `tsc --noEmit --listFiles` in `apps/api` now lists the 4 `db/seed` files; `pnpm typecheck` / `pnpm lint` green; `pnpm db:seed` output unchanged |
| B-004 | An unsigned request could permanently suppress a genuine Razorpay event: POST with a forged signature naming `evt_X` → `401`, then Razorpay's real `evt_X` → `200 {"status":"duplicate"}`, zero jobs, attacker's payload stored | T3 verification (Claude) | rejected deliveries were keyed on the caller-supplied `x-razorpay-event-id`, so an unauthenticated request took the `webhook_events.event_id` row first; the genuine delivery then hit `ON CONFLICT` and `reconciler.ts:50`'s `event.inserted` guard skipped the outbox enqueue. A 200 tells Razorpay not to retry, so the event was lost, not delayed | `ingressKey(claimedEventId, sha256, signatureValid)` in `apps/api/src/ingress/razorpay-webhook.ts:57-68`; unverified deliveries key into `unverified:<sha256(raw body)>` (D-031), the claimed id is logged at `razorpay-webhook.ts:154-158`, and C-C1 now states the invariant | verified | Repro before the fix: real delivery → `200 {"status":"duplicate"}`, stored row `{status: ignored, signature_valid: false, payload: <attacker's>}`, job count 0. After: live HTTP probe → forged row `unverified:4fbe5753…` (ignored, 0 jobs), `evt_live_victim` accepted with `process_event:evt_live_victim`. Regression test `apps/api/test/ingress.integration.test.ts` "does not let an unsigned request claim the idempotency key of a genuine event"; red-green confirmed by reverting only `ingressKey` (1 failed / 9 passed) and restoring it (10 passed) |
| B-006 | Two workers projecting `order.paid` and `payment.captured` for the same order and customer deadlocked (`40P01`); PostgreSQL aborted one transaction, the job retried after a full backoff, and nothing in the job's final state recorded that it happened | T4 verification (Claude) | ABBA lock-order inversion: `projectOrder` took `orders FOR UPDATE` then upserted `customers`, while `projectPayment`'s `ensureOrder` upserted `customers` then took `orders FOR UPDATE`. Razorpay delivers that event pair together, so `WORKER_CONCURRENCY=4` made the collision routine | split `ensureOrder` into `lockOrder` (before the customer upsert) and `createOrder` (after it, because `orders.customer_id` is itself an FK) in `apps/api/src/orchestrator/projections/payments.ts:57-88,106-117`; `C-C3` and D-035 now fix one global lock order | fixed | Red-green proved with the deterministic interleaving in `apps/api/src/orchestrator/projections/projections.test.ts` "does not deadlock when an order event and a payment event share an order and a customer": Codex's `payments.ts` restored → `40P01 deadlock detected` at `ensureOrder (payments.ts:66)`, `1 failed \| 7 passed`; fix restored → `8 passed`. Live probe (port 4031, worker enabled) posted both events concurrently → both `succeeded` at `attempts=1`, zero deadlock lines in the API log |
| B-007 | A `payment.*` and an `order.*` event for the same order could deadlock (`40P01`) during the cold-start absent-row window; the worker retried and hid the collision as `attempts=2` | T5 verification (Claude) | `lockOrder` was snapshot-dependent: an absent `SELECT ... FOR UPDATE` took no lock, allowing `projectPayment` to reach `customers` before the order identity was reserved while a sibling order transaction held an uncommitted order row | `apps/api/src/orchestrator/projections/shared.ts:lockOrderSlot` now locks an existing order or inserts a NULL-customer placeholder before either projection touches `customers`; `projectOrder` fills its own placeholder in place, and `projectPayment` links the customer before writing the payment. The global C-C3 order remains entity -> order -> customer | fixed | `pnpm --filter @aegis/api exec vitest run src/orchestrator/projections/projections.test.ts --no-file-parallelism --maxWorkers=1` -> 1 file / 9 tests passed, including `reserves an unseen order slot before the payment customer lock` |
| B-008 | `worker queue and projections > ignores an older subscription event and preserves its version` failed roughly one root `pnpm test` run in four with `version: 2, last_event_id: 'evt_sub_new'` instead of `version: 1` | T6 verification (Claude) | the test queued **both** subscription jobs at once and ran a `concurrency: 2` worker, so `FOR UPDATE SKIP LOCKED` decided which event was projected first. `isStaleSubscriptionEvent` only suppresses a write when a newer projection already exists; when the worker claimed `evt_sub_old` first, that event legitimately created the row and `evt_sub_new` then superseded it — a correct final state (`status='pending'`, `last_event_id='evt_sub_new'`) reached in two writes. The assertion pinned `version`, a write counter, to a scheduler outcome the worker never promised | extracted `drainJob(pool, jobId)` from `runOneEvent` and drained the two jobs in separate phases so the newer event is projected before the stale one is released (`apps/api/test/worker.integration.test.ts:102-121,432-437`); no production code changed | fixed | Order-dependence proven deterministically before the fix by running `processEventHandler` single-threaded in both orders: `evt_sub_new -> evt_sub_old` → `{"status":"pending","version":1,"last_event_id":"evt_sub_new"}`; `evt_sub_old -> evt_sub_new` → `{"status":"pending","version":2,"last_event_id":"evt_sub_new"}`. After the fix the file passed 5/5 isolated runs and the full gate 3/3. Non-vacuous: removing **both** precedence guards (the `isStaleSubscriptionEvent` branch at `subscriptions.ts:71` and the `EXCLUDED.last_event_at >= subscriptions.last_event_at` condition in the upsert `WHERE`) turns it red with `status: 'active', last_event_id: 'evt_sub_old', version: 2`; removing only the TypeScript guard leaves it green, because the SQL `WHERE` enforces the same precedence independently (defence in depth from `0003_projection_guards`) |
| B-009 | `x-aegis-chaos: llm_down` could not reach any LLM call that ran in the worker | T6 verification (Claude) | the chaos flag was stored in `AsyncLocalStorage` around the ingress HTTP handler, but the worker consumes a durable job in a separate async context | ingress now persists only the validated development/test literal `chaos: 'llm_down'` on the `process_event` payload; `processEventHandler` accepts that literal and re-enters `runWithLlmChaos` around the orchestrator call. Production ingress omits the field (C-D5) | fixed | `pnpm --filter @aegis/api exec vitest run test/chaos.integration.test.ts src/worker/process-event.test.ts --no-file-parallelism --maxWorkers=1` -> request persistence/production-gate tests plus the worker re-entry test pass; `apps/api/test/chaos.integration.test.ts` asserts `{ eventId, chaos: 'llm_down' }` in the queued payload and `apps/api/src/worker/process-event.test.ts` asserts `chaos_llm_down` with zero provider calls |
| B-010 | Every timestamp the diagnostician sends to the model was serialized as `{}`: `rzp_created_at`, `created_at`, and `updated_at` on the payments row and both customer timestamps silently lost their value in the prompt | T7 verification (Claude) | `maskPii` treated any non-array object as a record to recurse into (`apps/api/src/llm/mask.ts:79`). A `Date` has no own enumerable entries, so `Object.fromEntries(Object.entries(date))` returns `{}`. Harmless until T7, because T6 only ever masked parsed webhook JSON — `diagnostician.ts:151` is the first caller to mask a **projection row**, which comes straight from node-postgres with `timestamptz` columns as `Date` instances | pass `Date` through unchanged in `maskPii` (`apps/api/src/llm/mask.ts:78`) so `JSON.stringify` emits its ISO form; dates carry no PII | fixed | Probe on the real snapshot shape before the fix: `{"rzp_created_at":{},"created_at":{},"updated_at":{}}` inside `row`, and `{}` for both customer timestamps. After: `"created_at":"2026-09-05T10:00:00.000Z"`. Regression test `apps/api/src/llm/mask.test.ts` "preserves timestamps so projection rows keep their failure timing" asserts the round-tripped JSON, so a re-introduced recursion turns it red. Masking is unaffected: the same probe still shows `a••••@example.com` / `+91••••••3210` / `A••••` and no raw PII |
| B-011 | `pnpm x402:buy` failed before the API smoke test because the root script used a workspace-local `tsx`, then the script could not parse the nonce from the required `accepts[].extra` wire shape | T14 verification | the root package has no `tsx` binary and the challenge nonce is nested under `accepts[0].extra.nonce` | route the script through `@aegis/api`'s `tsx`, wrap it in an async `main`, and read `extra.nonce` (`package.json`, `scripts/x402-buy.ts`) | fixed | `API_URL=http://localhost:4100 pnpm x402:buy prod_001` → first `402`, paid `200`; `--replay` → replay `402 nonce_already_settled`; `--amount 999999` → `402 amount_exceeds_policy` |
| B-005 | API worker integration files could truncate the shared test database while another Vitest file was running | T4 | `fileParallelism=false` plus worker limits in the config did not reliably serialize the package invocation under Vitest 5; concurrent migration/projection hooks produced missing relations, FK failures, and timeouts | `apps/api/package.json` invokes Vitest with explicit `--no-file-parallelism --maxWorkers=1`; the config keeps the same limits as defense in depth | fixed | Before the script pin, root `pnpm test` failed with `relation \"jobs\" does not exist` and contaminated duplicate assertions. After it, `pnpm test` and the three full gates in `TestChecklist.md` passed at 23 API files / 128 tests |

| B-012 | `GET /api/v1/events`, `/actions`, `/subscriptions`, `/invoices`, `/disputes` returned a `next` cursor their own `before` parameter rejected, so no client could fetch a second page | T18 (Claude, while wiring "Load older events") | `String(row.created_at)` on a node-postgres `Date` yields `"Sat Sep 05 2026 15:29:57 GMT+0530 (India Standard Time)"`; the list queries validate `before` with `z.string().datetime()` → 400 `validation_error` | `cursorOf()` in `apps/api/src/routes/approvals.ts:38-47` returns `Date.toISOString()` for every list route | fixed | `apps/api/test/approvals.integration.test.ts` "returns an ISO cursor that the events and actions lists accept as `before`": three events, `limit=2` → `next` round-trips through `new Date(next).toISOString()` and the second page returns the remaining row; observed before the fix: `curl 'localhost:4000/api/v1/events?limit=2'` → `"next":"Sat Sep 05 2026 15:29:57 GMT+0530 (India Standard Time)"` |
| B-013 | The Overview's model-health tile showed "1 call, 100 % degraded" after 18 diagnoses | T18 (Claude, Chrome check of `/`) | `metrics.ts` aggregated `count(*)` over a subquery already grouped by provider, so `calls` counted providers and `degraded` counted providers with any degraded row | sum the per-provider counts (`sum(provider_count)`, `sum(degraded_count)`, latency as sum/count) in `apps/api/src/routes/metrics.ts:50-63` | fixed | `apps/api/test/approvals.integration.test.ts` "counts model calls per diagnosis, not per provider": 2 openai + 1 degraded fallback → `calls 3, degraded 1, avg_latency_ms 200, by_provider {openai: 2, fallback: 1}`; live after the fix: `{"calls":18,"degraded":18,"degraded_rate":1,…,"by_provider":{"fallback":18}}` |

| B-014 | Every simulated B2B invoice arrived with `floor_amount_paise = amount_paise`, so the negotiator could never offer a discount: `offerForRound` clamped to the floor, `discountPct` came out 0, `moneyImpactPaise` 0, the action auto-approved, and the approvals queue stayed empty. The module's whole point — a bounded discount a human signs off — was invisible | T19 (Claude, trying to verify the approvals queue against real data) | `projectInvoice` inserts `floor_amount_paise = amount_paise` and the simulator's B2B invoice fixture carries no merchant floor, so no invoice in the system ever had negotiating room | `merchantFloorPaise(notes, amount)` in `apps/api/src/orchestrator/projections/invoices.ts` reads `notes.floor_amount_paise`, validates it as integer paise in `[0, amount]` and otherwise falls back to the amount; `packages/shared/src/sim/invoices.ts` states a ₹3,57,000 floor on the ₹4,20,000 B2B invoice | fixed | Unit: `projections.test.ts` "accepts a stated floor inside the invoice amount and falls back to the amount otherwise" (string, zero, absent, above-amount, negative, fractional, non-numeric, null and array inputs). Live after the fix: `invoice_expired_b2b` → invoice floor 35700000, action `b2b_negotiator/discount_offer` `pending_approval` with `money_impact_paise -2100000`, bounds `floor_amount limit ₹3,57,000 actual ₹3,99,000 ✓` and `max_discount_pct limit 15 actual 5 ✓`; approving it in Chrome executed the offer |

| B-015 | Acknowledging a compliance flag crashed the page with `TypeError: Cannot read properties of undefined (reading 'indexOf')` in `EvidenceHighlight`; before that, every flag read "Product copy is not in the agent catalog" instead of showing the copy | T20 (Claude, Chrome check of `/compliance`) | Two holes in the same seam: the flag list carried no product copy (the page fell back to `/x402/catalog`, which only returns `agent_purchasable = true` products, and every risky seed product is `false`), and `POST /api/v1/compliance/flags/:id/status` answered with `RETURNING *` from `compliance_flags` alone, so replacing the row dropped the description the component reads | `GET /api/v1/compliance/flags` and the status route both join `products` and return `product_name`, `product_description`, `product_category` (`apps/api/src/compliance/routes.ts:36-46,55-66`); `splitOnSpan`/`EvidenceHighlight` accept a missing description and say so instead of throwing | fixed | `apps/api/test/approvals.integration.test.ts` "returns the flagged product copy with each compliance flag and keeps the filters working": asserts the description contains the evidence span verbatim, that `status`/`risk` filters still work after the join, and that the status decision answers with `product_name`/`product_description`; `compliance.test.ts` covers `splitOnSpan(undefined | null, …)`. Chrome after the fix: Acknowledge updates the card in place with the highlight intact and no console exception |

| B-016 | The dashboard could not change any guardrail: every save reported "The API at http://localhost:4000 is unreachable", while the same `PUT` from curl worked. The x402 Lab also never showed the decoded `X-PAYMENT-RESPONSE` | T21 (Claude, Chrome check of `/settings`) | CORS. `@fastify/cors` was registered with only an `origin`, so the preflight answered `access-control-allow-methods: GET, HEAD, POST` and the browser refused to send the `PUT` before it left the tab; `X-PAYMENT-RESPONSE` is not a CORS-safelisted response header, so `response.headers.get()` returned null in the browser although curl saw it | `apps/api/src/app.ts` registers cors with `methods: ['GET','HEAD','POST','PUT','OPTIONS']` and `exposedHeaders: ['X-PAYMENT-RESPONSE']` | fixed | `apps/api/test/app.test.ts` "allows the methods and exposes the headers the dashboard needs": preflight for `PUT /api/v1/guardrails/max_discount_pct` includes PUT, and a normal response carries `access-control-expose-headers: X-PAYMENT-RESPONSE`. Live: `curl -i -X OPTIONS … -H "Access-Control-Request-Method: PUT"` → `access-control-allow-methods: GET, HEAD, POST, PUT, OPTIONS`; in Chrome the save then succeeded and the toast read "Maximum discount updated · Recorded as human:dashboard" |
| B-017 | Flipping the kill switch changed the database but no dashboard noticed: the top-bar pill still read "Kill switch off" until a manual reload, and no `system.kill_switch` event ever reached the SSE stream | T21 (Claude, Chrome check of `/settings`) | `approvalRoutes` publishes `system.kill_switch` through an optional `bus`, and `app.ts` registered the plugin without one, so `options.bus?.publish(...)` was a silent no-op. C-B5 requires the stop to reach every consumer within one poll cycle | pass the shared bus when registering the routes (`apps/api/src/app.ts:93`) | fixed | `apps/api/test/approvals.integration.test.ts` "publishes system.kill_switch so every dashboard sees the stop at once": two toggles publish exactly two events with `{enabled}`, and an unrelated guardrail edit publishes none. Chrome after the fix: the pill turned red the moment the dialog was confirmed, with no reload |

| B-018 | White text on the accent fill measured 3.13:1 in dark mode, below the 4.5:1 the constraints require: the Run demo button, the active theme segment and the metrics window switcher | T22 (Claude, Lighthouse accessibility run on `/`) | `--accent` is a light blue in dark mode (`#5b8dff`), so white text on it cannot reach 4.5:1; the components hardcoded `text-white` for both themes | new `--on-accent` token (white in light, `#0b1020` in dark) applied wherever a fill uses the accent (`globals.css`, `Button`, `ThemeToggle`, `GlowInput`, the window switcher and the x402 stepper) | fixed | Lighthouse accessibility on `/` went from 96 (1 failed audit, three nodes at 3.13:1) to **100 with 0 failed audits**; `/approvals`, `/events` and `/x402` also score 100 on mobile |
| B-019 | `money.recovered_paise` was zero after 23 executed recovery actions and always had been: the `recovered_revenue` ledger account had never been written once, so the headline Revenue Recovery number could not be non-zero | T23 (Claude, building the demo storyboard) | not the attribution logic — the clock. `scripts/sim/cli.ts` stamped every fixture with the frozen `DEFAULT_SIM_CREATED_AT` (`1_757_000_000` = 2025-09-04), so deliveries arrived with `rzp_created_at` ~367 days in the past (`rzp_created_at 2025-09-04 21:03:33+05:30` vs `received_at 2026-09-06 07:32:55+05:30`) while actions executed at wall-clock now. `attributeRecovery` requires `a.executed_at <= $1 AND a.executed_at >= $1 - window` with `$1 = capturedAt`, so the candidate query returned nothing, silently, forever. A standalone `payment_captured_after_retry` also invents fresh entity ids that match no action (D-073) | the CLI stamps the current second and prints it, with `--created-at` to pin a reproducible run; `--order`/`--customer` pin the ids a scenario names and map onto `priorFailureOrderId`/`priorFailureCustomerId` for the retry capture; `pnpm demo` waits for the recovery action to reach `executed` before delivering the capture (`scripts/sim/cli.ts`, `scripts/sim/types.ts`, `scripts/simulate.ts`, `scripts/demo.sh`) | fixed | `apps/api/test/sim-cli.test.ts` 11 tests, red-green proved by restoring the frozen constant (1 failed / 10 passed) and reverting (11 passed). Live: `ledger_entries` went from zero `recovered_revenue` rows to `recovered_revenue \| 1 \| 79900` after the first fixed demo run, and `money.recovered_paise` now rises 79 900 per run (159800 → 239700 → 319600) |
| B-020 | `pnpm x402:buy prod_001` failed `bad_signature` on every purchase, so the documented agent-purchase command could not complete | T23 (Claude, first full demo run) | two defects in the client half: `scripts/x402-buy.ts` never loaded `.env`, so `X402_SIM_SECRET` and `X402_PAY_TO` fell back to the `.env.example` placeholders; and it signed `process.env.X402_PAY_TO ?? 'merchant:aegis-demo'` instead of the `payTo` the 402 challenge had just quoted (`merchant_aegis_demo`). `facilitator.ts:46` rejects `payload.payTo !== config.X402_PAY_TO` | load the repo `.env` as `simulate.ts` does, and take `payTo` from `accepts[0].payTo` (`scripts/x402-buy.ts`, D-074) | fixed | `pnpm x402:buy prod_001 --payer probe-b --replay` → `402` challenge, `200` with `paymentResponse: eyJzdWNjZXNzIjp0cnVlLCJ0eElkIjoi…`, replay `402 nonce_already_settled`. Before the fix the same command returned `402 bad_signature` then `402 invalid_payment_header` |
| B-021 | `pnpm demo` reported a previous run's action as the outcome of the step just executed (`discount_offer: blocked · bound hit: quiet_hours_local` after a run in which quiet hours were open) | T23 (Claude, comparing two consecutive demo runs) | `report_action` took the newest action of a kind from `GET /api/v1/actions`, and the worker had not yet created this run's row, so the newest was the previous run's | the read-back only accepts rows with `created_at >= STARTED_AT`, and the B2B offer gets a 45 s budget because its module drafts wording with the model before the guardrails are evaluated (`scripts/demo.sh:report_action`) | fixed | consecutive runs now print this run's own outcomes: `discount_offer: blocked · money impact ₹21000.00 · bound hit: daily_discount_budget_paise` with the correct bound, and `no action from this run yet` instead of a stale row when the worker is behind |
| B-022 | A `compliance_scan` job was re-queued while it was still running, so the same scan ran concurrently with itself, burned duplicate model calls, and climbed to `attempts=7` against its own `max_attempts=5` | T24 hardening drill (Claude, job accounting after severing every DB connection) | the sweeper's lease was a flat `locked_at < now() - interval '2 minutes'`, but a compliance scan makes one model call per active product and takes 137–669 s (measured). Past two minutes the sweeper could not tell a long job from a crashed worker, re-queued it, and a second worker claimed it. Two of the last four scan runs show `attempts=2` for the same reason | the lease is per kind: `LEASE_BY_KIND = { compliance_scan: '20 minutes' }`, everything else keeps two minutes (`apps/api/src/worker/sweeper.ts`) | fixed | `apps/api/test/worker.integration.test.ts` 14 tests; red-green proved by restoring the flat lease (`does not sweep a long-running job kind that is still inside its own lease` → `AssertionError: expected 2 to be 1`, both jobs swept) and reverting (14 passed) |
| B-023 | Pino's redaction covered `*.email`, `*.contact` and `*.phone` but not `card.number`, which C-D4 names explicitly; and because pino's `*` matches exactly one level, a top-level `logger.info({ email })` would have been written in clear | T24.1 security pass against Constraints §D (Claude) | the path list was written once against the shape of a request object and never re-checked against the fields C-D4 actually names | `LOG_REDACT_PATHS` in `apps/api/src/app.ts` now lists every named field at both depths (`email`, `*.email`, …, `card.number`, `*.card.number`) plus `x-payment` alongside the other credential-bearing headers | fixed | `apps/api/test/app.test.ts` "covers every field the constraint names, at the top level and one level in" (9 tests passed). No current call site logs a nested payload — `grep` for `log.*(payload|entity|customer` returns nothing — so this is defence in depth, not a leak that happened |

Template for a new bug:
```
| B-0NN | <symptom in one line> | <task/commit> | <what actually caused it> | <what changed, file:line> | open/fixed/verified | <command + output> |
| B-026 | The prism hero fell back to Canvas 2D on machines that could run WebGPU, and the WGSL drew a beam the geometry never described | hero pass (Claude), from the project owner's report that the hero never showed the WebGPU effect | two independent faults. (1) `detectRenderer()` returned `"2d"` whenever `prefers-reduced-motion: reduce` was set — a request to stop *animating* read as a request to stop using the GPU, and remote desktops, VMs and any user with the accessibility setting on got the 2D path on perfectly capable hardware. (2) `prism.wgsl` re-derived the entry ray from the fan's centre angle times a magic `1.8` instead of using `scene.entry.from`, so the WebGPU picture and the tested geometry disagreed by design | reduced motion now only freezes the animation (the shader already had a `still` uniform, and the loop skips redundant redraws); the renderer choice asks one question, whether the browser exposes WebGPU at all, and the real answer comes from `init()`. The uniform block carries the entry origin, the scene size and the extrusion, so the shader derives nothing | fixed | Chrome with a SwiftShader adapter → `webgpu` context and the shader renders; the same build with no adapter → `requestAdapter() returned null` and the 2D renderer. `prismShader.test.ts` fails if the WGSL struct and `prismUniforms` ever disagree again |
| B-027 | Prewarming the pipeline broke the very thing it was added to: every browser fell back to Canvas 2D | hero pass verification (Claude), first browser run after adding `compile()` | `await prism.compile(canvasSurface)` passes a `Surface` outside a frame, which throws `VGPU-SURFACE-NOT-IN-FRAME`; the one `try/catch` around the whole setup treated that as "this machine has no WebGPU" and quietly demoted the renderer. The console said `unavailable`, which is what made it look like a browser problem rather than our bug | compile against the surface's render signature (`{ colors: [canvasSurface.format] }`, the form the reference uses); and the fallback now wraps `init()` alone — anything after a successful device init logs `console.error` and names itself a component failure | fixed | before: `context: "2d"` on a browser with a working adapter; after: `context: "webgpu"` on the same build and browser |
| B-024 | The dashboard failed to compile the moment the sandbox store existed: `You're importing a module that depends on useSyncExternalStore into a React Server Component module` | T25 build (Claude, Chrome) | `lib/sandbox.ts` exported the `useSandbox` hook next to the pure store, and `lib/api.ts` (imported by every server page) imported the store for `sandboxRequestHeaders`; Turbopack rejects the React import on that path even though nothing on the server calls it. `tsc`, eslint and vitest were all green — only the bundler sees the RSC boundary | the hook moved to `lib/useSandbox.ts` (`"use client"`); `lib/sandbox.ts` has no React import and only guards `typeof window` | fixed | `next dev` overlay gone; `pnpm --filter @aegis/web build` exit 0; console clear on `/` |
| B-025 | The sandbox dialog rendered centred on the 56px top bar with its header off-screen (`top: -124px`), and in Live mode the top bar overflowed the 360px viewport (`scrollWidth 454`) | T25 build (Claude, Chrome) | `TopBar` has `backdrop-blur`; `backdrop-filter` makes an element the containing block for `position: fixed` descendants, so a modal rendered from inside the header was positioned against the header, not the viewport. Separately the pill's `Live · acc_…` label cost 153px on a bar with ~10px to spare (F-019 measured 342/357) | `SandboxPill` portals the modal to `document.body` (`createPortal`); the label is `md+` only and the state stays in the accessible name and colour, like the kill-switch pill | fixed | `[role=dialog]` rect `top 109 / bottom 414` in a 523px viewport; 360px iframe `scrollWidth 345 ≤ 360`, pill 31px, `aria-label` `Live · acc_ChromeLive1, open sandbox settings` |
```

## Deviations from the checklist

| ID | Task | What deviated | Why | Approved by |
|---|---|---|---|---|
| DV-001 | T1 | PostgreSQL install/role creation could not be executed by the agent | `sudo` requires a password on this VM (D-028); the script `scripts/bootstrap-system.sh` is ready and must be run by the human once | Architect (self), pending human run |
| DV-002 | T2 | Step 2.7's "unit — checksum mismatch" test is covered by the integration test `apps/api/test/schema.integration.test.ts` ("rejects checksum drift for an applied migration", real `aegis_test` database) instead of a unit test with a pool double. Codex had also reworded steps 2.3–2.8 to describe the implementation; the original wording was restored during verification and the "tick, never reword" rule was added to `AGENTS.md` | a real-database drift test is stronger evidence than a mocked pool; the step text must stay the spec so intent can be diffed against implementation later | Claude (Verification Agent), 2026-09-05 |
| DV-003 | T3 | Checklist step 3.3 specifies `eventId = headers['x-razorpay-event-id'] ?? 'sha256:' + sha256(raw)` for every delivery. That holds for verified deliveries; rejected ones are now keyed `unverified:<sha256(raw body)>`. Step 3.3's text is left unchanged | following the step literally lets an unauthenticated caller occupy a genuine event's idempotency key and drop the event (B-004); C-C1 outranks the step and now states the invariant | Claude (Verification Agent), 2026-09-05 |
| DV-004 | T4 | Checklist step 4.4 assumes `last_event_at` exists on orders/payments/invoices/disputes and `version` exists on disputes; the baseline schema had neither | D-032 adds these columns in `0003_projection_guards.sql`/`.down.sql`, preserving `rzp_created_at` as the entity creation timestamp and using `last_event_at` for event precedence | Architect ruling in HANDOFF4, 2026-09-05 |
| DV-005 | T5 | The pending `TestChecklist.md` §T5 examples used the stale `payment_failed_3ds` name and `--n`; the canonical scenario is `payment_failed_3ds_intl` and the burst flag is `--burst N`. Scenario builders also live in `packages/shared/src/sim/` with thin `scripts/sim/` re-export paths, rather than maintaining a second definition under `scripts/` | The handoff makes Checklist §5 authoritative and requires one shared definition for the CLI and dev route; the stale acceptance commands were rewritten and the rate-limit outcome is recorded separately as `rate_limited` | Architect ruling in HANDOFF6, 2026-09-05 |

| DV-006 | T5 | Checklist §5 lists the simulator flags as `--dupes N`, `--burst N`, `--seed S`, `--api URL`, `--chaos llm_down`. Verification added a sixth, `--contend`, plus `buildContendedBurst` in `packages/shared/src/sim/scenarios.ts`; the step text is unchanged | `--burst N` is specified as "N distinct events concurrently", and distinct events share no rows, so `max(attempts)=1` is true under *any* lock order — the check passed while proving nothing (D-041 overclaimed). `--contend` aims one burst at a single order and customer, which is the only shape that can form the B-006/B-007 cycle. It found B-007 on its first run | Claude (Verification Agent), 2026-09-05 |
| DV-007 | T7 | The checklist's `diagnosis/schema.ts` interface block describes a third copy of `ROOT_CAUSES`, `STRATEGIES`, and `DiagnosisSchema`; the implementation re-exports the prompt registry's single canonical definitions instead (D-045). Nested card fields are also masked recursively, and unapplied snapshots return a non-persisted deterministic result | Re-declaring the contract would make the stub blind to prompt drift. Persisting a diagnosis for a duplicate/stale projection would create a fresh row for an event that did not win precedence. Both choices preserve the higher-priority C-A2/C-A3/C-A6 boundaries; the checklist step text remains unchanged | Architect ruling D-045 / Task 7 handoff, 2026-09-05 |
| DV-008 | T8 | Checklist step 8.5 names `EventOrchestrator.test.ts`; the fake-module call-order, null-proposal, approval, idempotency, and crash-recovery coverage is consolidated in `apps/api/test/orchestrator.integration.test.ts` | These cases exercise the real PostgreSQL projection, action, audit, and advisory-lock boundaries together; the focused acceptance command still covers every specified behavior (10 files / 41 tests) | Architect ruling D-049/D-051, 2026-09-05 |
| DV-009 | T13/T14 | The checklist labels the ledger and x402 migrations `0003` and `0004`, but `0003_projection_guards.sql` already exists | Ledger uniqueness is numbered `0004_ledger_unique.sql`; x402 needs no migration because `x402_payments.nonce` is already UNIQUE. Both task steps remain unchanged and this numbering correction is recorded here | HANDOFF14 architect ruling, 2026-09-05 |
| DV-010 | T22 | Step 22.2 says to write the WGSL after `npx vgpu docs cat getting-started.md` and `npx vgpu examples search "fullscreen effect"`, and step 22 lists `three` among the dependencies. The docs and the example search were run (the example gallery returned only `gradient` for "fullscreen"); `three` was **not** installed, and the shader could not be device-validated on this machine | `vgpu/three` exists to bind loader-resolved WGSL into three.js TSL node materials, which this hero does not use: it is a single fullscreen `effect()`. Installing a 3D engine for a shader that never builds a scene would be an unused dependency (C-E4). `npx vgpu check src/components/prism/prism.wgsl` parses the shader and prints its reflection with `diagnostics: []`, but device validation is skipped with `VGPU-WGSL-VALIDATE-NO-DEVICE` because this VM has no WebGPU adapter (the same reason the browser falls back to Canvas 2D). The browser is the real check and it was run | Claude (Lead Frontend Developer), 2026-09-06 |
| DV-011 | T24 | Step 24.2's "Postgres restarted mid-run (`sudo systemctl restart postgresql` by the human)" was not run as written | `sudo` requires a password on this VM (DV-001), so the agent cannot restart the service. The closest equivalent was run instead: every API and worker connection to `aegis` was terminated with `pg_terminate_backend` while 242 jobs were in flight. That exercises the same code paths the drill targets — pool reconnect, `/health`, worker resume, job accounting — but does **not** cover the API surviving a period where the server is genuinely absent (connection refused rather than reset). A human should run the real restart once before the recording. | Architect (self), pending human |
| DV-012 | T24 | Step 24.2's duplicate storm `sim burst --n 200 --dupes 5` was run as `--burst 200 --dupes 5` and then twice more in runnable shapes | the flag is `--burst` (DV-005). At 200×5 the run is 1 200 deliveries against a 600/minute limiter and 200 model calls, so it failed closed twice — correctly. The property under test ("counts reconcile") was proven at depth (`--dupes 200`, one event, 201 deliveries → 1 job, `duplicate_count=200`) and at breadth on a clean instance (`--burst 80 --dupes 5` → `jobs=80/80 succeeded=80 max_attempts=1`). Recorded rather than silently substituted. | Architect (self) |
| DV-013 | T25 | The brief lists "Ask Aegis, Compliance, or Diagnostics if triggered manually" as routes that should honour `x-aegis-llm-key`. Ask honours it synchronously. A manual compliance scan honours it through the in-process registry: the job row carries only `secretFingerprint(key)` and the worker resolves the key from memory (`llmForFingerprint`), falling back to the `.env` client after a restart or eviction. Diagnoses are not switched: they run from webhook deliveries inside the worker, which carry no browser header | a durable `jobs` row must never hold a credential (C-D3), and a webhook has no caller to bring a key; the only path that keeps the secret out of PostgreSQL and still runs the manual scan on the caller's key is the fingerprint hand-off, which works because API and worker share one process (`server.ts`) | Claude (Lead Architect), 2026-09-06 |
| DV-014 | T25 | `POST /api/v1/sandbox/keys` accepts a webhook secret of 8–256 printable ASCII characters, shorter than the 16 the `.env` `RAZORPAY_WEBHOOK_SECRET` must have | the deployment secret is generated by the operator and can be made long; the sandbox secret is whatever the merchant already typed into the Razorpay dashboard, and refusing it would leave every delivery for that account unverifiable. The floor rejects trivially short values; the modal states the rule | Claude (Lead Architect), 2026-09-06 |

## The 2 AM log (failure recovery stories for the submission)

> Evaluators read "what broke at 2 AM, and how you got out". Record real incidents here as they happen: what broke, how it was noticed, how it was diagnosed, what fixed it, what changed so it cannot recur. Keep it honest.

| # | When | What broke | How we noticed | Diagnosis | Fix | Prevention |
|---|---|---|---|---|---|---|
| 1 | 2026-09-05 | Nothing runs on the box: no Node, no Postgres, and `sudo` wants a password so the agent cannot apt-install | first `which` sweep | environment, not code | user-level `nvm` for Node; privileged steps isolated into a single script for the human | `scripts/setup.sh` checks prerequisites and prints exactly what is missing |
| 2 | 2026-09-05 | First API boot printed the migration CLI's usage text and exited | `curl /health` returned nothing; the log showed `usage: migrate-cli` | a module with side effects at import time was imported for one constant | split constants into `db/paths.ts`; guard the CLI entry point (B-001) | rule for Codex: entry-point modules export nothing; shared constants live in side-effect-free modules |
| 3 | 2026-09-05 | `pkill -f 'tsx src/server.ts'` killed the verification shell itself (pattern matched the shell's own command line) | tool exit code 144, no output | `pkill -f` matches every process whose argv contains the pattern, including the parent shell | keep the child PID (`$!`) and `kill -TERM $PID` | `TestChecklist.md` uses PID-based shutdown everywhere |
| 4 | 2026-09-05 | `db/seed/seed.ts` passed every gate while never being typechecked: `pnpm typecheck` was green only because no tsconfig included it, and `tsc` cannot resolve `pg`/`dotenv` from `db/` at all | Verification Agent ran `tsc --listFiles` for the api program and saw only `db/seed/data/guardrails.ts` (pulled in by a test import); adding `../../db/seed` to `include` produced `TS2307: Cannot find module 'pg'` | `db/` is not a workspace package, so Node resolution from `db/seed` finds no `node_modules` with `pg`; the seed only ran because `tsx` falls back to its own install location | root devDependencies for `pg`, `dotenv`, `@types/pg` (D-030) + `db/seed` in the api typecheck and lint scripts (B-003) | AGENTS.md rule: every TS file outside `apps/*`/`packages/*` must be included by a package's typecheck and lint scripts |
| 5 | 2026-09-05 | The webhook ingress passed all 27 of its own tests and still had a hole big enough to silently drop a captured payment: forge one 401 naming an event id, and the genuine delivery for that id is answered `duplicate` and never processed | Verification Agent asked "who controls `event_id` at the moment of the upsert?" — the answer was "anyone with curl", because the header is read before the HMAC is checked | the tests covered *a* bad signature and *a* good signature, never a bad one followed by a good one with the same id; the idempotency key and the authentication check shared a namespace | key rejected deliveries into `unverified:<sha256(raw body)>` so the trusted key space is writable only by holders of the webhook secret (B-004, D-031) | C-C1 spells out the invariant; the regression test is red-green proved, and every future ingress path (T5 simulator, T24 hardening) must keep the namespaces separate |
| 7 | 2026-09-05 | The worker passed 12 adversarial concurrency tests and still had a lock-order inversion that made two workers kill each other's transactions on the single most common Razorpay event pair (`order.paid` + `payment.captured`) | Verification Agent asked "in what order does each projection take its rows?" and diffed the two answers: `orders → customers` against `customers → orders` | every test either raced two events for the *same* entity or drove one event at a time, so no test ever had two projections holding different rows at once; the failure mode was also self-healing (retry), so it would never have shown up as a red test — only as latency | one global lock order for all projections, stated in `C-C3` and D-035, plus a regression test that pins the interleaving deterministically instead of hoping a `Promise.all` collides (B-006) | a "no deadlock" claim needs a lock-order argument, not a passing concurrency test; T8/T10/T11 must extend the order in `C-C3`, never invert it |
| 6 | 2026-09-05 | Launching worker and projection integration files together caused timeouts and missing rows despite `fileParallelism=false` | Vitest's pnpm process created one worker per file, so each suite's shared `aegis_test` `beforeEach` truncate could interleave | pinned the API package command to one worker while retaining the serial-file setting (B-005) | the full API suite now runs deterministically; the worker suite explicitly covers the same race at the application level |
| 8 | 2026-09-05 | The simulator's burst check reported `max(attempts)=1` as proof that the T4 lock-order fix held. It could not have reported anything else: `--burst N` builds N *distinct* events, so a 50-event run produced 50 payments across 50 distinct orders and 50 distinct customers and no two jobs ever touched the same row | Verification Agent asked what the assertion would have to see to fail, then checked the database: `SELECT count(DISTINCT order_id), count(DISTINCT customer_id)` over the burst's payments returned 50 and 50 | a green concurrency check is not a lock-order argument; the shape of the fixtures decides whether the assertion can fail at all, and nothing in the output said which shape had run | `--contend` builds a burst that aims every event at one order and one customer, `verifyBurst` prints `contended=true\|false` next to the number, and a unit test pins the contended fixture shape (D-042) | on its first run `--contend` found B-007: `payment.*` and `order.*` still deadlock when the order row is not yet visible to the payment transaction, a window the T4 regression test skips because it pre-creates both parents. `pg_stat_database.deadlocks` 1 → 2, `jobs.last_error` = `deadlock detected` |
| 9 | 2026-09-05 | The Task 7 gate still failed once after the serial script fix because a second Vitest process was running against `aegis_test` concurrently | the API suite reported a diagnosis insert deadlock and a projection FK failure while a focused verification process was active | the package command was deterministic, but two independent commands still shared the database; this was test orchestration, not a production path | stopped the overlapping process, then ran the focused suite and three full gates one at a time | the handoff records the failed trial; future verification must ensure one `aegis_test` owner before claiming a gate |
| 10 | 2026-09-05 | The diagnosis prompt had been quietly throwing away every timestamp it was given. `maskPii` returned `{}` for each `Date`, so the model was asked when a payment failed and handed `"created_at":{}` five times per prompt | Verification Agent stopped reading `mask.ts` and ran it on the shape it actually receives in production — a `RETURNING *` payments row plus a `CustomerRow` — instead of the hand-written JSON literal in the test | the unit test only ever masked plain JSON objects, which is what a parsed webhook body is. T7 was the first task to mask a **database row**, and node-postgres returns `timestamptz` as `Date`, an object with no own enumerable entries. Nothing failed: the recursion produced valid JSON, the prompt validated, the diagnosis came back, and the timing was simply gone | a `Date` passthrough in `maskPii` plus a regression test that asserts the round-tripped JSON, not the object (B-010) | a masking test that builds its own literal proves only that the literal is masked. Every masking test from here on feeds `maskPii` a value with the same provenance as production — a driver row, not a fixture — and asserts on `JSON.stringify` output, because that string is what the provider actually receives |
| 11 | 2026-09-05 | The first contended-burst run exposed a cold-start ABBA window after B-006 had fixed only existing parents | the deterministic projection interleaving held an uncommitted order row while the payment path reached the customer upsert; the old path could deadlock, and the worker hid the collision behind a retry | an absent-row `FOR UPDATE` does not reserve an identity | `lockOrderSlot` now inserts/locks a NULL-customer order placeholder before customer upserts; the focused projection suite passes 9/9 with the new cold-start regression | D-048 makes the reservation part of C-C3's global order, and the contended-burst probe remains a standing worker-level check |
| 12 | 2026-09-05 | The `x-aegis-chaos: llm_down` header disappeared between ingress and the worker, so a chaos drill could not force a worker-side diagnosis failure | the flag lived only in request `AsyncLocalStorage`, while `process_event` was consumed in a later worker context | request-local state does not cross a durable queue boundary | ingress persists the single validated `llm_down` mode on the job payload and `processEventHandler` restores it with `runWithLlmChaos` before the orchestrator call (B-009, D-050) | `pnpm --filter @aegis/api exec vitest run test/chaos.integration.test.ts --no-file-parallelism --maxWorkers=1` -> 1 file / 5 tests passed; `src/worker/process-event.test.ts` -> 1 file / 2 tests passed; production remains gated at ingress |
| 13 | 2026-09-06 | The first end-to-end demo from the dashboard reported every diagnosis as `degraded`: 12 × `openai_InternalServerError 5xx: 503`, then 6 × `circuit_open` | the model-health tile on the Overview (once B-013 made it count calls instead of providers) | the OpenAI-compatible proxy still lists models but answers 503 for `gpt-5.6` and `gpt-5.4-mini`; only the `gpt-5.6-luna/sol/terra` ids complete a chat request. The resilient client did exactly what C-A4 asks: fell back to the rule-based diagnosis, opened the breaker after three failures, and every action still got proposed and bounded | `.env` now names a served model (`OPENAI_MODEL=gpt-5.6-luna`, `OPENAI_MODEL_FAST=gpt-5.6-luna`); `.env.example` keeps the generic defaults | the LLM pill in the top bar and the model-health tile make a silent fallback visible within one page load; T24 should add the proxy probe to `scripts/setup.sh` |
| 14 | 2026-09-06 | "Load older events" could never load: the API's own cursor failed the API's own validator (B-012) | first click in Chrome → `400 validation_error` toast | nothing had ever requested a second page; every T13 test read one page | ISO cursors everywhere plus a paging regression test | a list endpoint's test must round-trip its cursor |
| 15 | 2026-09-06 | The approvals queue was empty no matter what the simulator sent, and every negotiation showed a ₹0 discount | building `/approvals` and having nothing real to render | the demo data could not produce the state: `floor = amount` on every simulated invoice leaves the negotiator exactly zero room, so its guarded offer is always "pay the full amount" and never crosses the approval limit. Every unit test still passed, because they pass their own floors | the merchant floor now comes from the invoice notes and the B2B fixture states one (B-014) | a page that renders a state must be verified against data that can actually reach that state; "the queue is empty" is not evidence that the queue works |
| 16 | 2026-09-06 | Clicking Acknowledge on a compliance flag replaced the card with a Next.js error overlay | Chrome, first click on the finished page | the list route and the status route returned different shapes for the same row. The page trusted the second one and lost the product copy the highlighter reads. Types did not catch it: both were typed `Record<string, unknown>` on the server and the client type described only the list shape | both routes now return the joined product copy, and the highlighter tolerates its absence (B-015) | when two endpoints answer with "the same row", one query shape should serve both — and the client type is the contract to check them against |
| 17 | 2026-09-06 | Two features that passed every server-side test were dead in the browser: guardrail edits ("the API is unreachable" while curl worked) and the live kill-switch indicator | clicking Save on `/settings`, then watching the top-bar pill stay green after stopping everything | both failures lived in the wiring, not the logic. CORS advertised only `GET, HEAD, POST`, so the browser dropped the `PUT` before sending it and hid `X-PAYMENT-RESPONSE`; and the approvals plugin was registered without the event bus, so its kill-switch publish was an optional-chaining no-op | explicit `methods` and `exposedHeaders` (B-016), and the bus passed to `approvalRoutes` (B-017) | an optional dependency that silently disables a safety signal deserves a test that asserts the signal, not the call. Both now have one, and browser verification is part of every frontend task |
| 18 | 2026-09-06 | The single number a Revenue Recovery submission is judged on had never been non-zero. 23 executed recovery actions, and the `recovered_revenue` ledger account was empty | building the demo storyboard: checked the ledger before writing "money recovered" into the README | not the attribution SQL — the simulator's clock. Every delivery was stamped `1_757_000_000` (2025-09-04) while actions executed at wall-clock 2026-09-06, and attribution requires `executed_at <= capturedAt`. The join returned nothing, with no error, on every run since T13 | live timestamps by default plus `--order`/`--customer` so a retry names the failure it recovered; the demo waits for `executed` before delivering the capture (B-019, D-072, D-073) | a test suite proves the code does what you wrote; it cannot prove the system can reach the state you built it for. Same class as B-014. Any KPI that has never been non-zero in a live run is a bug until proven otherwise |
| 19 | 2026-09-06 | The first full demo run died at the x402 step: every purchase came back `bad_signature`, while the dashboard's x402 Lab worked perfectly | running `scripts/demo.sh` end to end for the first time | the Lab signs server-side (D-067) so it never exercised the client path. `x402-buy.ts` never loaded `.env` and signed a `payTo` it guessed from its own environment, which stopped matching the moment `.env` diverged from `.env.example` | load `.env`; sign the `payTo` the challenge quoted (B-020, D-074) | a documented command is part of the product. `pnpm demo` now runs the client path on every rehearsal, so it cannot rot silently again |
| 20 | 2026-09-06 | A compliance scan was being run twice at once by two workers, and its job had climbed to `attempts=7` against a `max_attempts` of 5 | T24 job accounting: after severing every database connection with 242 jobs in flight, the totals reconciled — but one row stood out with attempts above its own limit | the sweeper's lease is what tells a crashed worker from a slow one, and it was a flat two minutes. A compliance scan makes one model call per active product and runs for 137–669 s, so the sweeper re-queued it mid-run every two minutes and another worker picked up the same scan | per-kind leases; `compliance_scan` gets 20 minutes (B-022) | a lease is a claim about how long the work takes. Any new job kind has to declare one, or the sweeper will decide for it — and it will be wrong for exactly the jobs that matter most |
| 21 | 2026-09-06 | The duplicate-storm drill from the checklist could not run as written: `sim burst --burst 200 --dupes 5` is 1 200 deliveries against a 600/minute limiter, and every one of the 200 accepted jobs makes a ~10 s model call | running the T24.2 drills in order | two separate mistakes in the drill, not the code. The limiter throttled 900 deliveries — correctly — and the 200-job backlog then meant nothing could complete inside the burst verifier's 30 s window. It also poisoned the next drill: an `order_paid` storm reported `succeeded=0/80` purely because it was queued behind the first one | run the storm under the limiter's budget and on an event kind that does not call the model, then reconcile the counts | a drill has to be sized for the system's real limits, or it measures the drill instead of the system. The simulator was right to fail closed both times |
| 22 | 2026-09-06 | The Sandbox / BYOK feature passed `tsc`, eslint and 48 web tests and then failed to compile in the browser: the whole dashboard was a red overlay | opening `/` in Chrome for the T25 check (`You're importing a module that depends on useSyncExternalStore into a React Server Component module`) | the store module exported a React hook next to its pure functions, and `lib/api.ts` — imported by every server page — imported that module for the request-header helper. Only Turbopack enforces the RSC boundary; the type checker and the unit tests never see it | the hook moved into its own `"use client"` module (B-024) | a module imported by `lib/api.ts` may not import React at all; the browser check and `next build` are part of every frontend task for exactly this reason — `pnpm typecheck` is not a build |
| 23 | 2026-09-06 | The sandbox dialog opened with its title off the top of the screen, and the Live pill pushed the 360px top bar into horizontal scroll | measuring `[role=dialog].getBoundingClientRect()` in Chrome (`top: -124px`) and the 360px iframe (`scrollWidth 454 > 357`) | `backdrop-filter` on the top bar makes it the containing block for `position: fixed`, so a dialog rendered from a header button is centred on the header; and the top bar had about 10px of slack at 360px before the new pill's 153px label | `createPortal` to `document.body`; icon-only pill below `md` with the state in the accessible name (B-025) | a modal is rendered from a portal, never from inside a blurred/transformed ancestor; any new top-bar control is measured at 360px before it ships — the bar's budget is written down in F-019 |
| 24 | 2026-09-06 | The hero's WebGPU effect "never rendered", and the first instinct — that the fallback logic was trigger-happy — was only half right | the project owner reporting a flat hero on a supported browser | the machine under test has no GPU, so `requestAdapter()` returns `null` and the fallback was doing its job. Forcing Chrome's software WebGPU (`--enable-unsafe-swiftshader`) put the real path on screen in seconds, and it turned out the shader *was* running — it just looked nothing like the reference. Two genuine faults hid behind the environment: a `prefers-reduced-motion` check that disabled the GPU path outright, and a shader that re-derived the entry ray from a magic constant (B-026) | reduced motion freezes the animation instead of changing renderer; the geometry feeds the shader every point it draws; the shader was rebuilt against the official vgpu example's techniques (D-082) | "it does not render" is a claim about an environment as much as about code — reproduce it with a software adapter before touching the fallback, and read pixels (`vgpu/node` + `target.read()`) rather than judging a shader by eye |
| 25 | 2026-09-06 | The fix for a false fallback *caused* a false fallback: adding pipeline prewarming sent every browser to Canvas 2D | the browser check straight after the change — `context: "2d"` on a machine whose adapter had just worked | `compile()` given a `Surface` outside a frame throws `VGPU-SURFACE-NOT-IN-FRAME`, and the single `try/catch` around device creation *and* our own setup reported it as an unsupported browser (B-027) | compile against the render signature; the fallback now wraps `init()` only, and a failure after that is a `console.error` that says it is our bug | one `catch` must not span two failure classes. "The environment cannot do this" and "our code is wrong" need different logs, or every bug arrives disguised as a limitation |
| 26 | 2026-09-07 | The replica rendered the prism, the beam, the bloom and the caustic — and no spectrum at all | first headless render of the rewritten shader: a perfect picture with the rainbow simply missing | not a shader bug. An equilateral prism deviates light by at least ~37°, so the beam arriving from the upper left met the far wall at 40.9° against a critical angle of 40.8° and totally internally reflected: physically, nothing could exit. Every wavelength failed `refract()` and the fan summed to zero | the entry geometry was solved on the CPU instead of guessed: a sweep over prism rotation, beam elevation and entry point looking for a configuration where the whole visible band escapes. The beam now rises at 24°, near minimum deviation, which leaves 13° of headroom below the critical angle — and that headroom is what lets the dispersion constant open a 16.7° fan | when a physically-derived picture comes out empty, check the physics before the code. A 40-line CPU port of the shader's own maths answered in one run what tweaking constants would not have |
| 27 | 2026-09-07 | The hero drew into a 300×150 swapchain and was upscaled by CSS onto a 990×289 box | reading `canvas.width` in the browser after the rewrite: the context was live, the picture was a stretched thumbnail | vgpu enables auto-resize only for a canvas that already reports a numeric `clientWidth` when the surface is created. Inside a freshly mounted React subtree it does not, so the surface was treated as buffer-only and kept the canvas element's default 300×150 | the surface is created with an explicit `size` and resized from a `ResizeObserver`; sizing is the renderer's job, not a heuristic's | a probe can lie: `canvas.getContext("webgpu")` returning a context proved nothing here, because the probe call is what created it. Measure the backing store, not the handle |
| 28 | 2026-09-07 | The new glass mesh rendered as polished chrome: a bright opaque solid instead of dark glass, and two rounds of darkening the material barely changed it | first headless render of the mesh pipeline | the Fresnel term used `clamp(dot(normal, view), 0, 1)`. Every back face of a solid we deliberately see *through* has a normal pointing away, so that clamp returned 0, which is a 90-degree incidence, which is a full mirror. Half the prism was being shaded as a perfect reflector, and no amount of tuning the tint could reach it | `abs(dot(normal, view))`: a face seen from behind is still glass, not a mirror | when two rounds of tuning a material barely move the picture, the term being tuned is not the term that is wrong. Read the shading maths for the case the geometry actually presents — here, faces seen from the inside |
